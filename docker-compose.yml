version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: studentsync_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_logs:/data/db
    restart: unless-stopped

volumes:
  mongo_logs:
